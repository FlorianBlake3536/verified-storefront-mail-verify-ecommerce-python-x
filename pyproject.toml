[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "verified-storefront-mail"
version = "0.1.0"
description = "Email-verified checkout, receipts, fulfillment, and delivery lookup in a typed Python service."
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "pydantic[email]>=2.8,<3", "uvicorn>=0.30,<1"]

[project.optional-dependencies]
test = ["pytest>=8.2,<9"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

